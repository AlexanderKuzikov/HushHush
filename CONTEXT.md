# CONTEXT — HushHush Project

> Этот файл — главный источник истины для быстрого восстановления контекста между сессиями и при смене AI-модели.

---

## 📌 Суть проекта

**HushHush** — десктопное приложение для Windows 10/11.  
Цель: кастомный слайдер для демонстрации фото/картинок из выбранного каталога.  
Замена стандартному Photos slideshow — с исправлением его багов и расширенным контролем.

---

## 🛠 Стек

| Слой | Технология | Причина выбора |
|---|---|---|
| Backend / логика | Go | Быстрое чтение ФС, горутины для предзагрузки, маленький бинарник |
| Frontend / UI | HTML + CSS + JS | Знакомый стек, полный контроль над внешним видом |
| Desktop bridge | Wails v2 | Нативный WebView2 (встроен в Win10/11), сборка в один .exe без Electron |
| Настройки | JSON (`%APPDATA%`) | Простое хранение конфига |

**Отвергнутые варианты:**
- ❌ Electron — тяжёлый (~150MB), сложная сборка
- ❌ NW.js — те же проблемы что Electron
- ❌ PyQt6 — не принципиально отвергнут, но Go предпочтительнее

---

## ✅ Согласованный функционал

### Обязательно (MVP)
- [x] Выбор папки с изображениями
- [x] Полноэкранный показ изображений
- [x] Настраиваемый интервал смены кадра
  - Слайдер / поле ввода прямо в UI
  - Горячие клавиши `+` / `-` во время показа
  - Диапазон: 1 сек — 60+ сек
- [x] Предзагрузка следующих 2–3 кадров в фоновом горутине
- [x] Зацикливание слайд-шоу
- [x] Режим Shuffle (случайный порядок)
- [x] Запоминание последней папки и настроек
- [ ] Чистый экран — UI-элементы скрыты в режиме показа
- [ ] Элементы управления появляются только по клику или `Esc`

### Желательно (v2)
- [ ] Эффекты переходов (fade, slide)
- [ ] Поддержка нескольких папок
- [ ] Исключение отдельных файлов из показа
- [ ] Показ имени файла / даты (опционально)
- [ ] Пауза при движении мыши
- [x] Drag & Drop папки на окно
- [ ] Разные интервалы для разных папок

### Поддерживаемые форматы
JPG, JPEG, PNG, GIF, WebP, AVIF

---

## 🐛 Баги Photos (которые мы исправляем)

| # | Баг | Наше решение | Статус |
|---|---|---|---|
| 1 | Зависание при переходе к следующему изображению «без причины» | Предзагрузка 2–3 следующих кадров в Go-горутине | 🟡 Базовая архитектура создана |
| 2 | Всплывающие элементы управления и полоски поверх изображения | UI показывается только по явному действию пользователя | 🔴 Ещё не доведено: сейчас панель появляется при движении мыши |

---

## 📁 Текущая структура проекта

```
HushHush/
├── main.go              # Точка входа, инициализация Wails-приложения
├── app.go               # Основная логика: чтение папки, фильтрация, настройки
├── preloader.go         # Фоновая предзагрузка изображений (горутины)
├── wails.json           # Конфигурация Wails
├── go.mod
├── build/
│   └── windows/         # Иконка, манифест
├── frontend/
│   ├── index.html
│   ├── package.json
│   ├── vite.config.js
│   ├── src/
│   │   ├── main.js      # Инициализация
│   │   ├── slider.js    # Навигация, интервал, горячие клавиши
│   │   └── ui.js        # Управление видимостью элементов UI
│   └── src/styles.css
├── README.md
└── CONTEXT.md           # Этот файл
```

---

## 📅 История сессий

### Сессия 1 — 2026-07-19
- Определён стек: Go + Wails v2
- Согласован функционал MVP и v2
- Зафиксированы баги Photos для исправления
- Созданы README.md и CONTEXT.md
- README обновлён до полного профессионального вида с бэджами и актуальными версиями
- Проверены версии стека: Go 1.26.5, Wails v2.12.0, Node.js v24 LTS
- Создана базовая структура проекта: `main.go`, `app.go`, `preloader.go`, `wails.json`, `go.mod`, frontend на Vite
- Реализованы: выбор папки, чтение изображений, таймер, shuffle, loop, сохранение конфига, базовая предзагрузка, drag & drop
- Выявлено отклонение от ТЗ: UI пока показывается при движении мыши, это нужно исправить
- **Новое правило:** обновлять `CONTEXT.md` после каждого существенного шага
- **Следующий шаг:** довести UI-логику до согласованного поведения (чистый экран, показ панели только по клику / `Esc`), затем проверить локальный запуск и добавить `go.sum`

---

## 🔜 Следующий шаг

> **Исправить UI-логику:** убрать появление панели по движению мыши, оставить показ только по явному действию (`click` / `Esc`), затем проверить локальный запуск и зафиксировать результат.

---

## 📋 Соглашения

- Язык кода: Go (backend) + JS (frontend)
- Язык комментариев и документации: русский
- Конфиг хранится в `%APPDATA%/HushHush/config.json`
- Ветка разработки: `main` (до появления фич-веток)
- Коммиты: `тип: описание` (например `feat: предзагрузка изображений`)
- `CONTEXT.md` обновляется после каждого существенного шага
